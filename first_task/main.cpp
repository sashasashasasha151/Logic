#include <iostream>
#include <string>

using namespace std;

int main() {
    string input_string;
    cin >> input_string;
    string terminate = input_string.substr(input_string.find("|-")+2);
    string input
    return 0;
}